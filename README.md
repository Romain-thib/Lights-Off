# 💡 LightsOff

LightsOff est une application JavaFX éducative inspirée du jeu de réflexion. Le but du jeu est simple : éteindre toutes les lumières sur une grille de 5x5 cases. Mais attention, chaque clic inverse l'état de la case sélectionnée ainsi que celui de ses voisines !

## Fonctionnement

- La grille est initialisée avec toutes les lumières éteintes.
- Un nombre aléatoire de cases est activé au démarrage pour créer une configuration de départ.
- En cliquant sur une case, celle-ci et ses voisines (haut, bas, gauche, droite) changent d'état (allumée ↔ éteinte).
- Le jeu se termine lorsque toutes les cases sont éteintes.

## Objectifs pédagogiques

Ce projet a été conçu à des fins éducatives afin d'illustrer les concepts suivants :

- La création d'interfaces graphiques avec JavaFX.
- La gestion des interactions utilisateur via les événements.
- L'application du modèle MVC (Modèle-Vue-Contrôleur) pour une architecture claire et maintenable.
